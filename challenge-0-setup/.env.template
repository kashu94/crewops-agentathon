# =============================================================================
# Crew Ops Agent-a-thon — Environment Variables
# Fill in values from deploy.sh output
# =============================================================================

# Azure Subscription
AZURE_SUBSCRIPTION_ID=
RESOURCE_GROUP=crewops-agentathon-rg

# AI Foundry
FOUNDRY_RESOURCE_NAME=
PROJECT_NAME=crewops-project
FOUNDRY_ENDPOINT=
PROJECT_CONNECTION_STRING=
MODEL_DEPLOYMENT_NAME=gpt-5.4
# Optional deploy.sh overrides (GlobalStandard supports gpt-5.4)
# MODEL_NAME=gpt-5.4
# MODEL_VERSION=2026-03-05

# Optional: a second, cheaper deployment for the Triage Agent (see
# challenge-1-build/config.py). Leave unset to reuse MODEL_DEPLOYMENT_NAME.
# TRIAGE_MODEL_DEPLOYMENT_NAME=

# Application Insights & Monitoring
APPLICATIONINSIGHTS_CONNECTION_STRING=
APPINSIGHTS_INSTRUMENTATION_KEY=

# Tracing (set to true to enable GenAI tracing)
AZURE_EXPERIMENTAL_ENABLE_GENAI_TRACING=true
OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT=true

# Challenge 4 — Portal Workflow (set after creating the workflow in Foundry portal)
# Build -> Workflows -> New workflow -> deploy -> copy the agent name here
WORKFLOW_AGENT_NAME=
